This directory preserves the historical SQLite migration history for audit and
desktop transition reference.

These migrations are not active for hosted PostgreSQL runtime and must not be
applied to PostgreSQL.
