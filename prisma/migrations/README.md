This is the active PostgreSQL migration history for hosted web runtime.

Use `npx prisma migrate deploy` against a clean PostgreSQL database.
Do not copy the archived SQLite migrations back into this directory.
